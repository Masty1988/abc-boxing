<svg width="1200" height="800" xmlns="http://www.w3.org/2000/svg">
  <!-- Fond dégradé -->
  <defs>
    <linearGradient id="grad" x1="0%" y1="0%" x2="100%" y2="100%">
      <stop offset="0%" style="stop-color:#1e1e1e;stop-opacity:1" />
      <stop offset="100%" style="stop-color:#2a2a2a;stop-opacity:1" />
    </linearGradient>
  </defs>

  <!-- Rectangle de fond -->
  <rect width="1200" height="800" fill="url(#grad)"/>

  <!-- Bordure -->
  <rect x="20" y="20" width="1160" height="760" fill="none" stroke="#ef4444" stroke-width="4" stroke-dasharray="10,10" opacity="0.3"/>

  <!-- Icône boxe -->
  <circle cx="600" cy="350" r="80" fill="#ef4444" opacity="0.2"/>
  <text x="600" y="390" font-family="Arial, sans-serif" font-size="100" fill="#ef4444" text-anchor="middle">🥊</text>

  <!-- Texte principal -->
  <text x="600" y="500" font-family="Arial, sans-serif" font-size="36" font-weight="bold" fill="#ffffff" text-anchor="middle">ABC BOXING</text>
  <text x="600" y="550" font-family="Arial, sans-serif" font-size="24" fill="#999999" text-anchor="middle">Photo à venir</text>

  <!-- Note -->
  <text x="600" y="620" font-family="Arial, sans-serif" font-size="16" fill="#666666" text-anchor="middle">Uploadez vos photos via le dashboard /taz</text>
</svg>
